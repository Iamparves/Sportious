import Hero from '../Hero/Hero';
import League from '../League/League';

const Home = () => {
    return (
        <main>
            <Hero />
            <League />
        </main>
    );
};

export default Home;